apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-app-service
spec:
  selector:
    app: {{.Release.Name}}-app
  ports:
    - port: {{.Values.ports.app}}
      targetPort: {{.Values.ports.app}}
  type: LoadBalancer